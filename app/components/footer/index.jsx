import React from 'react';

export default React.createClass({
  render() {
    return (
      <footer className="footer">
        This is a global footer you can add or remove.
      </footer>
    );
  }
});
